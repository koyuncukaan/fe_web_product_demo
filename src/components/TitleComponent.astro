---
const { html, titleJs } = Astro.props;
---

<script is:inline define:vars={{ titleJs }}>
  if (titleJs) {
    titleJs.forEach((fnc) => window.globalScripts.push(fnc));
  }
</script>

<Fragment set:html={html} />
